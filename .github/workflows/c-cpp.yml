name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  stresstest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: install dependencies
      run: |
        pip3 install tqdm
        git submodule update --init --recursive
    - name: stresstest
      run: /usr/bin/time ./stresstest.py

  bench:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: install dependencies
      run: |
        git submodule update --init --recursive
    - name: benchmark
      run: /usr/bin/time ./bench.sh